# 🚀 Golang Project Template

<div align="center">
	<a href="README.md">EN</a> | <strong>RU</strong>
</div>

<br />

<div align="center">
    <a href="/LICENSE"><img src="https://img.shields.io/badge/license-MIT-blue.svg?style=flat-square" alt="License"></a>
    <img src="https://img.shields.io/badge/go-1.24+-00ADD8?style=flat-square&logo=go" alt="Go Version">
</div>

<br />

Это готовый к продакшену шаблон для микросервисов на Go, построенный на принципах **Гексагональной архитектуры** (Ports and Adapters). Включает в себя настройку конфигурации, логирование, graceful shutdown, Docker и CI/CD.

## 📋 Содержание

- [0. Требования и инструменты](#0-требования-и-инструменты)
- [1. Начало работы](#1-начало-работы)
- [2. Структура проекта](#2-структура-проекта)
- [3. Разработка и Тестирование](#3-разработка-и-тестирование)
  - [Swagger документация](#swagger-документация)
  - [Горячая перезагрузка (Live Reload)](#горячая-перезагрузка-live-reload)
  - [Генерация моков (Mocks)](#генерация-моков-mocks)
  - [Линтер](#линтер)
- [4. Деплой](#4-деплой)
- [5. Лицензия](#5-лицензия)

---

## 0. Требования и инструменты

Перед началом работы убедитесь, что у вас установлены **Go (1.24+)** и **Docker**.

Проект использует стандартные инструменты экосистемы Go для удобства разработки.

### 🛠 Установка инструментов разработки

**1. Air (Live Reload)**
Позволяет автоматически пересобирать и перезапускать сервер при изменении кода.

```bash
# Через Homebrew (macOS)
brew install air

# Или через Go install
go install github.com/air-verse/air@latest
```

**2. GolangCI-Lint (Линтер)**
Инструмент для статического анализа кода.

```bash
# Через Homebrew (macOS)
brew install golangci-lint

# Или через Go install
go install github.com/golangci/golangci-lint/cmd/golangci-lint@v1.64.0
```

**3. Mockgen (Тестирование)**
Генерирует мок-объекты для интерфейсов (необходимо для unit-тестов).

```bash
go install go.uber.org/mock/mockgen@latest
```

---

## 1. Начало работы

### 1.1. Настройка окружения

Создайте локальный файл с переменными окружения из примера:

```bash
make env
```

### 1.2. Локальный запуск

Для запуска приложения в режиме разработки:

**Вариант А: С горячей перезагрузкой (Рекомендуется)**
Требует установленного `air`.

```bash
make run-watch
```

**Вариант Б: Обычный запуск Go**

```bash
make run-local
```

Сервер запустится по адресу `http://localhost:8080`.
Проверка здоровья: `curl http://localhost:8080/health`

---

## 2. Структура проекта

Проект следует стандарту Go Project Layout и принципам Гексагональной архитектуры.

```
├── cmd/
│   └── api/                # Точка входа в приложение (main.go)
├── config/                 # Файлы конфигурации (development, production, local)
├── deployments/            # Dockerfiles и скрипты деплоя
├── internal/
│   ├── adapter/            # Слой Адаптеров (Взаимодействие с внешним миром)
│   │   ├── inbound/        # Входящие запросы (HTTP REST, gRPC)
│   │   └── outbound/       # Исходящие вызовы (База данных, API)
│   ├── app/                # Логика инициализации приложения (wiring)
│   ├── core/               # Слой Бизнес-логики
│   │   ├── domain/         # Доменные сущности
│   │   ├── port/           # Интерфейсы (Порты) для взаимодействия
│   │   └── service/        # Реализация бизнес-логики
│   └── pkg/                # Общие пакеты (хелперы ответов, валидация)
└── Makefile                # Команды автоматизации
```

---

## 3. Разработка и Тестирование

### Swagger документация

Документация API формируется автоматически на основе комментариев в коде с помощью инструмента [swag](https://github.com/swaggo/swag).

- **Доступ:** `http://localhost:8080/swagger/index.html` (включено в dev/local окружениях).
- **Разработка:** Для ускорения локальной разработки документация **не** перегенерируется при каждом запуске. Если вы изменили комментарии к API, обновите её вручную:
  ```bash
  make swagger
  ```
- **Сборка:** Команда `make build` автоматически обновляет документацию перед компиляцией, гарантируя, что в релиз попадет актуальная версия.

### Горячая перезагрузка (Live Reload)

Используйте `air` для отслеживания изменений. Конфигурация находится в файле `.air.toml`.

```bash
make run-watch
```

### Генерация моков (Mocks)

Мы используем `go generate` и `mockgen` для создания моков.
Файлы моков генерируются в папку `internal/core/port/mocks`.

Чтобы обновить все моки после изменения интерфейсов:

```bash
make mocks
```

### Линтер

Для проверки качества кода перед коммитом:

```bash
make lint
```

### Запуск тестов

Запуск всех unit-тестов с проверкой на состояние гонки (race detection):

```bash
make test
```

---

## 4. Деплой

### Сборка Docker образа

Используется multi-stage сборка для создания минималистичного и безопасного образа (на базе `scratch`).

```bash
make docker-build
```

---

## 5. Лицензия

Распространяется под лицензией MIT. Подробнее см. в файле `LICENSE`.
